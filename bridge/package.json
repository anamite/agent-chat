{
  "name": "hermes-bridge-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Schema tooling for the Hermes Mobile Gateway wire protocol. Generates protocol.schema.json from protocol.ts (the Zod contract).",
  "type": "module",
  "scripts": {
    "gen:schema": "tsx gen_schema.ts",
    "gen:pydantic": "tsx gen_schema.ts && python3 gen_pydantic.py"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
