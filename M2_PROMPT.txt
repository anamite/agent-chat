M2 — Files & Voice

Read BUILD_PLAN.md §4.2 (Files & voice) and BUILD_PLAN.md §2 (App stack — all expo packages).

Implement:

1. APP — Install missing expo packages (using npx expo install, NOT npm install):
- expo-audio (AudioRecorder + AudioPlayer — NOT expo-av, it's removed in SDK 56)
- expo-file-system
- expo-image-picker
- expo-document-picker
- expo-image
- react-native-svg (for waveform)

2. APP — Create src/media/recorder.ts:
- useAudioRecorder() hook wrapping expo-audio's AudioRecorder
- Record to m4a, compute waveform peaks (64 max, normalize 0-1)
- requestRecordingPermissionsAsync

3. APP — Create src/media/player.ts:
- useAudioPlayer() hook wrapping expo-audio's AudioPlayer
- Play/pause/stop, current position, duration, seek
- Playback speed control

4. APP — Create src/transport/files.ts:
- uploadFile(uri, pairing): chunked upload to POST /files with Bearer auth
- downloadFile(fileId, pairing): download with Range support via expo-file-system
- Image caching strategy

5. APP — Create src/sdui/blocks/VoiceBlock.tsx:
- Waveform visualization using react-native-svg (horizontal bars, 64 peaks)
- Play button, progress bar, scrub handle
- Optional transcript display below waveform
- Duration label
- Uses the design tokens (dark theme, lime accent for play button)

6. APP — Create src/sdui/blocks/FileBlock.tsx:
- Image files: show inline using expo-image (rounded, max height, tap to fullscreen)
- Documents: show chip with filename, size, icon (PDF, file, etc.)
- Voice: delegate to VoiceBlock

7. APP — Update Composer in brain.tsx:
- Add attach button (📎) that shows action sheet: Photo, File, Voice
- Photo → expo-image-picker
- File → expo-document-picker
- Voice → push-to-talk recording (long press = record, release = send)
- Send file frames after upload completes (fileId in the block)

8. APP — Update BlockRenderer (create src/sdui/BlockRenderer.tsx if missing):
- Registry mapping block.type → component
- TextBlock, FileBlock, VoiceBlock at minimum

9. BRIDGE — Add voice transcription to hermes.py or a new whisper.py:
- When a message contains a voice block, download the audio, transcribe via Hermes' faster-whisper
- Hermes has faster-whisper built-in — check if there's a CLI or Python API at ~/.hermes/
- Feed transcript as user text to the Hermes agent (include a note "[user sent a voice note]")
- Echo transcript back into the voice block stored in frame_log

10. BRIDGE — Add voice synthesis to hermes.py:
- When agent uses ui_voice tool (M3 feature), synthesize via edge-tts
- For now, add the edge-tts based synthesis as a utility function in whisper.py
- edge-tts should be installed via pip

11. BRIDGE — Update schema/config for voice mime types (already has audio/mp4, audio/m4a, audio/mpeg, audio/ogg, audio/webm)

Install edge-tts with: uv pip install edge-tts

Test: voice imports resolve, file upload/download endpoints still work.

Work in /home/hermes/hermes-mobile-gateway. npm/npx/node at ~/.hermes/node/bin/.